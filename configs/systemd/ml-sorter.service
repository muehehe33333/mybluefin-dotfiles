[Unit]
Description=Micro-ML Background File Sorter
After=local-fs.target

[Service]
Type=oneshot
# Hardware efficiency lock (CPU only)
Environment="CUDA_VISIBLE_DEVICES="
Environment="OMP_NUM_THREADS=1"
# Execute via justfile
ExecStart=/usr/bin/sh -c 'cd %h/mybluefin-dotfiles && just tidy'
